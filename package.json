{
  "name": "it-asset-inventory-management",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --check backend/src/server.js && node scripts/check-role-contract.js && node scripts/check-password-policy.js && node scripts/check-masterdata-integration.js && node scripts/check-api-contract.js && node scripts/check-operational-integrity.js && node scripts/check-sql-placeholders.js && node scripts/check-xlsx-export.js && node --check dist/app.js",
    "start": "docker compose up --build --remove-orphans",
    "start:detached": "docker compose up -d --build --remove-orphans",
    "stop": "docker compose down",
    "logs": "docker compose logs -f mysql backend frontend",
    "dev": "docker compose -f docker-compose.dev.yml up --build --remove-orphans"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "dijkstrajs": "^1.0.1",
    "lucide-react": "^0.544.0",
    "qrcode": "^1.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "vitest": "^3.2.4"
  }
}
