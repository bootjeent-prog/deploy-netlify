# Legacy backend disabled

โฟลเดอร์นี้คงไว้เพื่อรักษาโครงสร้างโปรเจกต์เดิมเท่านั้น ระบบ Production ไม่เรียกใช้ Backend ชุดเก่า

Backend ที่ใช้งานจริงอยู่ที่ `backend/src/server.js` และเชื่อมต่อ MySQL ผ่าน `docker-compose.yml`
